import React from 'react';
import { SafeAreaView, Text, View } from 'react-native';

const Support = (props) => {
  return (
    <SafeAreaView>
      <View>
        <Text>Support</Text>
      </View>
    </SafeAreaView>
  );
};

export default Support;
